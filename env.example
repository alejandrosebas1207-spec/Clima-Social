API_TOKEN=tu_token_de_kobo_aqui
ASSET_ID=tu_asset_id_aqui
PORT=3000

# Nombres EXACTOS de las columnas en Kobo (no la etiqueta que ve el
# encuestador, sino el "name" interno de la pregunta). Búscalos en la
# consola del navegador (F12) dentro del objeto que imprime console.log(datos).
CAMPO_ENCUESTADOR=C_digo_encuestador
CAMPO_SUPERVISOR=C_digo_Supervisor
